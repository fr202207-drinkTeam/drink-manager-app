import { render, screen } from '@testing-library/react';
import AddItem from './AddItem';

// test('renders learn react link', () => {
//   render(<AddItem />);
//   const linkElement = screen.getByText(/商品追加/i);
//   expect(linkElement).toBeInTheDocument();
// });
